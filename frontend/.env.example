# Base URL for the API gateway.
# In development this is omitted — Vite's dev server proxy handles /api → localhost:8080.
# In production set this to the publicly accessible gateway URL.
VITE_API_BASE_URL=
